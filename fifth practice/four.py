#!/usr/bin/python3
import os, sys
import re




#Write a function that receives as a parameter the path to an xml document and an
#attrs dictionary and returns those elements that have as attributes all the keys
#in the dictionary and values ​​the corresponding values. For example, the elements
# that have the attributes:
# class = "url" and name = "url", "name": "url-form", " "url-form" and date-id = "item".
